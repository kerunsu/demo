# Optional: real speech analysis (FunASR Paraformer), etc.
# Used when config/analyzers.yaml speech mode is real and USE_REAL_ANALYZERS conditions are met.
# Install: pip install -r requirements-optional-analyzers.txt
# Large packages; not included in requirements.txt by default.

torch>=2.0.0
torchaudio>=2.0.0
funasr>=1.0.0
modelscope>=1.11.0
librosa>=0.10.0
