# M7-A frontend runtime environment template.
# Copy to frontend/.env.production or set in the host environment before build.

VITE_API_BASE_URL=http://SERVER_LAN_IP:3001/api
VITE_WS_URL=ws://SERVER_LAN_IP:3001/ws
