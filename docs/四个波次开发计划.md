一、现状判断：并非「全是假的」，但离你要的「真实分析」还有明显差距
对照代码与 docs/FINAL_CAPABILITY_MATRIX.md，当前视频/语音相关能力可以分成三层：

能力	默认开发配置	实际在算什么	是否算「真实分析」
注意力
ATTENTION_PROVIDER=local
浏览器低帧率采帧 → FaceDetector（有则用）+ 人脸框是否居中 → 后端 LocalAttentionObservationProvider 透传
半真实：有真摄像头、真人脸检测，但不是眼动/凝视，只是「脸是否在画面中央」的粗启发式
表达性语言
随 STT 走
languageFeatureService 只从转写文本算：有无说话、句长、句数、空回复、STT 置信度等；browserAudioCapture 的 RMS 仅用于 UI，未入库
基本是假的：没有响度、清晰度等声学特征
STT
VOICE_STT_PROVIDER=local
Python Voice Service + Vosk，可真实转写
可真实（需启动 voice-service 和模型）
TTS
VOICE_TTS_PROVIDER=local
Piper 本地合成
可真实
自然语言对话
AI_CHAT_PROVIDER=rule
规则回复；OpenAI Provider 已实现但未默认启用
架构完整，对话智能默认是 mock
情绪
EMOTION_PROVIDER=local
由答题/注意力/语言汇总指标推出来的启发式占比
假的（非独立情绪模型）
结论：

不能简单说「视频语音分析全是假的」——STT/TTS、摄像头采帧、粗粒度人脸朝向在开发环境下可以走真实路径。
但按你的目标标准：
注意力：现有实现不够（无可靠「是否看向屏幕/我们」）
表达性语言：当前基本是「转写统计代理」，没有声学分析
自然语言对话：接口齐全，默认仍是规则/mock，需同事接入真实 LLM
二、我能否独立完成注意力 + 表达性语言（简单版）？
可以独立完成，且不需要外部 API Key、不需要把儿童数据上云。范围与边界如下：

注意力（增强「是否看向屏幕」）
方案（建议）：浏览器端增强 + 沿用现有 descriptor 管线

现有链路已打通：browserCameraCapture → descriptor → behaviorFrameIngressService → 聚合 → /server snapshot → 报告。

改进方向（保持简单）：

在 extractVisualFeatures 中增强判定：
保留 FaceDetector 做人脸检测
增加人脸框几何特征：相对画面中心偏移、面积占比（过近/过远）、宽高比
可选：引入轻量 @mediapipe/tasks-vision Face Landmarker 估算鼻尖相对人脸框位置 → 比纯居中更接近「朝向屏幕」
输出统一的 roughlyFacingScreen + confidence + algorithmVersion（如 browser-attention-v2）
后端 LocalAttentionObservationProvider 基本不用改，继续消费 descriptor
默认 ATTENTION_PROVIDER=local；mock 保留给测试/降级
不做： 临床级注意力、精确眼动点、服务端存原始帧。

表达性语言（声学特征，简单版）
方案：浏览器 Web Audio API 按 voice turn 计算特征 → 写入 LanguageObservation

在 browserAudioCapture 录音结束时，对 turn 音频计算：

特征	计算方式	用途
响度 RMS / dB
AnalyserNode 时域
是否足够大声、是否几乎无声
语音活动占比
能量阈值统计
有效发声 vs 静音
清晰度代理
过零率 ZCR 或简单频谱质心
含糊/噪声大时偏低
时长
已有
与现有字段对齐
接入点：

新增 audioFeatureService.ts（或扩展 languageFeatureService）
在 STT 完成路径（speechSttService.persistSttLanguageFeatures 或 voice turn 完成回调）合并声学特征
扩展 LANGUAGE_FEATURE_KINDS（如 audio_loudness_rms、audio_speech_ratio、audio_clarity_proxy）
更新 behaviorAggregationService、reportScoringService.computeExpressiveLanguageScore、monitorSnapshotService
备选： 在后端对已合并的 webm 做同样分析（Python 或 Node），但浏览器端更实时、与现有采集一致。

三、自然语言对话：架构是否完整？同事如何对接？
当前事实：架构已完整，可直接对接。

儿童端录音
mediaIngress
speechSttService
transcript + languageFeatures
chatService / voiceTurn
llmSafetyGateway
voiceOrchestrator
ChatProvider
TtsProvider
回复文本
音频播放 /robot
关键文件：

编排：backend/src/services/voice/voiceOrchestrator.ts
对话入口：backend/src/services/chatService.ts
安全网关：backend/src/services/llmSafetyGatewayService.ts
Provider 接口：backend/src/services/voice/types.ts、shared/src/providers.ts
已有实现：ruleChatProvider、openAiChatProvider
文档：docs/SPEECH_LLM_PIPELINE.md、docs/NEXT_STAGE_VOICE_LLM_GUIDE.md
同事最小对接步骤：

实现 ChatProvider（或直接用 openAiChatProvider）
.env 设置 AI_CHAT_PROVIDER=openai（或新 provider 名 + runtime 注册）
可选：AI_TTS_PROVIDER=openai 或继续 VOICE_TTS_PROVIDER=local（Piper）
不改 chatService 主流程——必须经过 runChildSafeLlmTurn / reviewAssistantResult
验收：GET /api/voice/providers、GET /api/health、完整 voice turn E2E
本轮我只需补一份 docs/LLM_INTEGRATION_HANDOFF.md（对接清单、接口契约、env、测试命令），不实现真实 LLM。

四、本轮开发规划（按你给的优先级重排）
你说波次 B/C（现场验收）先不做，核心是真注意力 + 真表达性语言 + LLM 架构交接。建议拆成以下波次：

波次 0：稳定性收尾（1 天，仍最高优先）
来自原文档 §6，与后续真实分析无冲突，应先做：

提交 manifest 并发修复（rawMediaPersistenceService.ts + 测试）
验证 /server snapshot 在 RAW_MEDIA_PERSISTENCE=enabled 时不因损坏 manifest 失败
（可选）Python Voice Service HTTP 健康探测——对 STT/TTS 联调有帮助，建议保留
交付： 稳定基线 + npm run build && npm test 通过

波次 1：真实注意力增强（2–3 天）
步骤	内容
1.1
增强 frontend/src/features/camera/browserCameraCapture.ts 的 extractVisualFeatures
1.2
明确 algorithmVersion，区分 browser-face-detector-v1 → v2
1.3
确认 ATTENTION_PROVIDER=local 为开发默认；mock 仅测试/降级
1.4
单测：有人脸朝屏 / 无人脸 / 偏左偏右 / 多脸 / 摄像头不可用
1.5
替换 /server：AttentionGauge、注意力曲线读真实 roughlyFacingScreen 聚合
1.6
替换报告：reportScoringService 注意力维继续使用 screenOrientedRatio，标注 provider/algorithm
验收： 儿童端开摄像头训练一轮 → /server 曲线随转头变化 → 报告注意力分非固定 mock 值

波次 2：真实表达性语言（声学特征）（2–3 天）
步骤	内容
2.1
扩展 shared/src/behaviorObservations.ts 的 LANGUAGE_FEATURE_KINDS
2.2
前端 browserAudioCapture：turn 结束时计算 RMS、speech ratio、clarity proxy
2.3
新 API 或扩展现有 voice turn 完成接口，上报 audioFeatures metadata
2.4
后端 audioFeatureService / 扩展 languageFeatureService 写入 LanguageObservation
2.5
更新 behaviorAggregationService 汇总声学指标
2.6
更新 computeExpressiveLanguageScore：文本特征 50% + 声学特征 50%（权重可配置常量）
2.7
替换 /server：语音流水线区展示响度/清晰度条（替代纯 STT 状态）
2.8
替换报告：表达性语言维显示 provider 与降级标注
验收： 大声清晰说话 vs 小声含糊 → 表达性语言分与监控指标有可区分差异

波次 3：LLM 对接交接（0.5–1 天，文档为主）
新建 docs/LLM_INTEGRATION_HANDOFF.md
内容：Provider 接口、env 模板、安全网关约束、voice turn 时序、测试清单、禁止事项（无 key 不上云）
检查 GET /api/voice/providers 返回字段是否足够同事自检
不修改 默认 AI_CHAT_PROVIDER=rule（除非你们明确要求）
波次 4：逐步替换与标注（贯穿 1–2，持续）
在 /server 与报告 V2 中：

所有指标展示 provider / algorithmVersion / degraded
mock 路径保留但 UI 明确标「降级/模拟」
更新 docs/FOUR_STAGE_PROGRESS_AND_NEXT_PLAN.md 对应条目
本轮明确不做
波次 B：LAN 双主机现场验收
波次 C：媒体加密、运维 cron、断网复测文档
真实情绪模型、真实 LLM 叙事、临床/专业评分常模
复杂 gaze 模型、云端视觉 API
五、风险与合规声明（实现时必须保留）
注意力：仍是「任务参与度/朝向屏幕」启发式，不能写成临床注意力或精确凝视。
表达性语言：声学特征是工程代理指标，不能替代专业语言评估。
浏览器兼容：FaceDetector 仅 Chromium 系；需 graceful 降级到几何/质量启发式。
隐私：继续 descriptor-only，不上传原始视频帧；声学特征只传标量，不传原始音频到分析接口（音频仍走已有 media ingress）。
六、建议执行顺序与工期
波次 0（稳定性）  →  波次 1（注意力）  →  波次 2（表达性语言）  →  波次 3（LLM 交接文档）
     1 天                 2–3 天                  2–3 天                    0.5–1 天
合计约 5–8 个工作日（单人串行）；波次 1 与 3 可部分并行（文档不依赖注意力代码）。

七、下一步
你确认这份规划后，建议从 波次 0（manifest 修复提交 + 验证） 开始，然后进入 波次 1 注意力增强。

如需调整，请说明：

注意力是否接受「浏览器 FaceDetector + 几何启发式」，还是必须上 MediaPipe？
表达性语言特征是否必须在浏览器算，还是允许后端对合并音频分析？
LLM 交接文档是否需要指定同事用 OpenAI，还是保持 provider 中立？