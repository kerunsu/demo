/**
 * 旧「高级 YAML /server?view=config」前端已下线。
 * 配置改走 /server/config/*（config_algo.js）；本文件保留为空壳以免历史引用报 404。
 * API（/api/server/config*、presets、apply 等）仍由后端提供。
 */
(function () {
  console.info("[server.js] legacy config UI retired → /server/config/overview");
})();
