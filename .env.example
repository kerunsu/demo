# 复制为 .env 并按需修改（.env 已被 .gitignore 忽略，勿提交密钥）
# 详见 docs/ENVIRONMENT.md、docs/ROBOT_DEPLOY.md

# Flask
SECRET_KEY=change-me-in-production

# 分析器模式：config/analyzers.yaml 默认 global.mode=real
# 无模型/本机冒烟可：USE_REAL_ANALYZERS=false 或服务端控制台选 mock_only
# USE_REAL_ANALYZERS=true

# 录制与媒体（须与 static/js/child.js 中分辨率、采样率一致）
# VIDEO_FPS=30
# VIDEO_WIDTH=640
# VIDEO_HEIGHT=480
# AUDIO_SAMPLE_RATE=16000
# AUDIO_CHANNELS=1

# 儿童端媒体采集：
#   browser = 本机联调捷径（/child getUserMedia + 可选 C2）
#   agent   = 生产路径（robot_runtime 独占采流，推荐课堂部署）
# CHILD_MEDIA_MODE=agent
# CHILD_MEDIA_AGENT_PORT=19091
# CHILD_MEDIA_AGENT_KEY=

# TODO: re-enable after testing — 跳过「整场录制未启动」浏览器 alert（默认 1）；设 0 恢复
SKIP_RUNTIME_RECORDING_CHECK=0

# 机械臂控制：server_osc | child_agent | robot_runtime（跨机推荐）
# ROBOT_CONTROL_MODE=robot_runtime
# ROBOT_RUNTIME_KEY=

# 分析开关
# POSE_ESTIMATION_ENABLED=true
# FACE_ANALYSIS_ENABLED=true
# AUDIO_ANALYSIS_ENABLED=true

# 日志
# LOG_LEVEL=INFO

# ===== 儿童对话 / 浏览器 TTS（2026-07-22）=====
# browser=关掉预录，用浏览器朗读；file=旧预录；both=调试叠播
DIALOGUE_TTS_MODE=browser
DIALOGUE_ENABLED=true
# 默认由教师端按钮唤醒；仅需保留语音唤醒时设为 true（Server 控制台也可持久化设置）
DIALOGUE_WAKE_WORD_ENABLED=false
AI_CHAT_PROVIDER=asd
# 共用 ExpertAnnotator / DemoRobot 的 LLM key（勿把密钥写进仓库）
ASD_AGENT_ENV_FILE=../ExpertAnnotator_ASD-main/asd_llm_agent/.env
# FunASR：优先本进程；否则走本地 voice-service（app.py 默认自动拉起 :8765）
VOICE_PYTHON_SERVICE_URL=http://127.0.0.1:8765
# START_VOICE_SERVICE=true
# VOICE_SERVICE_PYTHON=../ExpertAnnotator_ASD-main/asd_llm_agent/.venv/Scripts/python.exe
# VOICE_SERVICE_STT_PROVIDER=local-funasr
# 首次运行 app.py 时自动安装 FunASR 依赖并下载三套本地模型；已有缓存会跳过
# VOICE_SERVICE_AUTO_INSTALL=true
# VOICE_SERVICE_AUTO_DOWNLOAD=true
# VOICE_SERVICE_MODEL_DOWNLOAD_RETRIES=2
# VOICE_SERVICE_MODEL_DOWNLOAD_TIMEOUT=1800
# VOICE_SERVICE_FUNASR_MODEL=C:\Users\skr\.cache\modelscope\hub\models\iic\speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch
# VOICE_SERVICE_FUNASR_VAD_MODEL=C:\Users\skr\.cache\modelscope\hub\models\iic\speech_fsmn_vad_zh-cn-16k-common-pytorch
# VOICE_SERVICE_FUNASR_PUNC_MODEL=C:\Users\skr\.cache\modelscope\hub\models\iic\punc_ct-transformer_cn-en-common-vocab471067-large
# 可选：显式指定本机 modelscope 缓存模型目录（本进程 FunASR）
# FUNASR_MODEL_PATH=C:\Users\skr\.cache\modelscope\hub\models\iic\speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch

# ===== LAN HTTPS（儿童端麦克风 getUserMedia 需要安全上下文）=====
# 先生成证书: .\scripts\generate_lan_cert.ps1
# 然后二选一启用（启用后本进程只听 HTTPS:8080，HTTP 不可用）：
# ENABLE_HTTPS=true
# 或显式路径：
# SSL_CERTFILE=.runtime/certs/cert.pem
# SSL_KEYFILE=.runtime/certs/key.pem
# 教师端 Vite 代理到 HTTPS 后端时（可选）：
# VITE_BACKEND_URL=https://127.0.0.1:8080
